# Outputs

`s2-cli` produces outputs in the following formats:

1. **Terminal Console (Stdout/Stderr):** Human-readable, colorized output for statuses, logs, and webhook event streaming.
2. **Subprocess Execution:** Wraps and outputs logs from underlying command-line invocations (e.g., passing through `railway logs`).
